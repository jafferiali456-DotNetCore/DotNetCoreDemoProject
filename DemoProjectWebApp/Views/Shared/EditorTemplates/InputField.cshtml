@using Kendo.Mvc.UI
@model string

@(Html.Kendo().DropDownListFor(m => m)
    .Name("FormInputField")  // Must match Model field name
    .OptionLabel("Select..")
    .DataTextField("Text")
    .DataValueField("Value")
    .BindTo((IEnumerable<SelectListItem>)ViewData["actions"])
)
